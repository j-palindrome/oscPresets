import { Config } from 'tailwindcss'

const config: Config = {
  content: [
    './components/**/*.tsx' // Adjust the file extensions as needed
  ],
  theme: {
    extend: {}
  },
  plugins: []
}

export default config
